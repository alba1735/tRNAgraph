# Generated by python-semantic-release on every release. Formatting it here would fight the
# generator: semantic-release rewrites the file from its own template, so the two would take
# turns reformatting it and every release would carry a spurious whitespace diff.
CHANGELOG.md

# Log output, not source.
.log/
